import { createStyles, makeStyles } from "@material-ui/core";

export const useStyles = makeStyles((theme) => (
  createStyles({
    playButton: {
      width: "100px"
    },
    container: {
      marginTop: '50px'
    }
  })
));
